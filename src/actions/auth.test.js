import {login, authError, authRequest, authSuccess, storeA} from './auth.js';

describe('login', () => {
    // it('should login', () => {
    //     const fetch = fetch
    //         .once(JSON.stringify({ authToken: '1234567' }))
    //         .once(JSON.stringify({username: 'A', password: '12345'}));
    //     const dispatch = jest.fn();
    //     return login({ username: 'A', password: '12345' })(dispatch)
    //     .then(() => {
    //         expect(dispatch).toBeCalledWith(authRequest);

    //     })
    //     .catch(() => {
    //         expect(dispatch).toBeCalledWith(authError)
    //     })
    // })
})
import React from 'react';
import {shallow, mount} from 'enzyme';

import  {Dashboard} from './dashboard';

describe('<Dashboard />', () => {
    it('renders', function () {
        //shallow(<Dashboard />)
    });
})